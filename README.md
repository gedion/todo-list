# todo-list managed by AI

Minimal Hello World CLI

Run locally:

- Python: `python hello.py` (or `./hello.py` after `chmod +x hello.py`)

Optional: Run in Docker (if Docker is available):

- Build: `docker build -t hello-world-cli .`
- Run: `docker run --rm hello-world-cli`
